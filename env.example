# Environment Configuration
# This application does NOT require any environment variables to run!

# Environment
NODE_ENV=development

# Note: OpenAI API key is now provided by users on the frontend
# No environment variables are required for the application to run
# Users input their own API keys through the web interface

# The application is designed to work immediately without any configuration
# Each user provides their own OpenAI API key when they want to use AI features
